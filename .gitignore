__pycache__/
*.py[cod]
*.pyo

# local environment overrides — never commit real credentials
.env
upload-to-synology.sh

# downloaded model weights — large binary files, not source code
models/

# Python packaging artefacts
*.egg-info/
dist/
build/

# macOS metadata
.DS_Store
